//Esteban Lau koo homework2

#include <stdio.h>

int main() {

printf("Hello World!\a");

return 0;

}

// closing the quote in printf causes error missing terminating " character 
// removing the closing brace at the very end : expected declaration or statement at end of input
// change printf to printf : implicit declaraction of function 'print'; did you mean 'printf'?
// remove the semicolon at the end of the printf line : epected ';' before 'return'
// \n : new line \t : tab \\ : backslash \" : double quote \b : backspace \r : Carriage return \a : Alamrm or Beep